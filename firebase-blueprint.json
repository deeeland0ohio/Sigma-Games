{
  "entities": {
    "Message": {
      "title": "Message",
      "description": "A single chat message with potential soft/hard deletion markers",
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier of the message"
        },
        "text": {
          "type": "string",
          "description": "The textual content of the message"
        },
        "senderName": {
          "type": "string",
          "description": "The display nickname of the user"
        },
        "senderId": {
          "type": "string",
          "description": "Unique user session identifier"
        },
        "createdAt": {
          "type": "number",
          "description": "Timestamp in milliseconds when message was created"
        },
        "os": {
          "type": "string",
          "description": "The operating system used by the sender"
        },
        "isDeleted": {
          "type": "boolean",
          "description": "Flag indicating user or admin deletion of the message"
        },
        "isPlaceholder": {
          "type": "boolean",
          "description": "Is message a placeholder after deletion"
        },
        "isSystemInfo": {
          "type": "boolean",
          "description": "Is message a system alert message"
        },
        "isAdminDeleted": {
          "type": "boolean",
          "description": "Has this been soft-deleted by an admin"
        },
        "isPermanentlyRemoved": {
          "type": "boolean",
          "description": "Has this been permanently hard-deleted by an admin"
        }
      },
      "required": ["id", "text", "senderName", "senderId", "createdAt", "os"]
    },
    "UserPresence": {
      "title": "UserPresence",
      "description": "Presence and state metadata for active chat participants",
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "User session identifier corresponding to document ID"
        },
        "nickname": {
          "type": "string",
          "description": "Chosen user handle"
        },
        "lastActive": {
          "type": "number",
          "description": "Server timestamp or Client-updated epoch in milliseconds of last active signal"
        },
        "isTyping": {
          "type": "boolean",
          "description": "Typing status indicator"
        },
        "isOwner": {
          "type": "boolean",
          "description": "Is this user verified as Sigma Dev"
        },
        "os": {
          "type": "string",
          "description": "The user's client operating system"
        }
      },
      "required": ["id", "nickname", "lastActive", "isTyping", "isOwner", "os"]
    },
    "BanRecord": {
      "title": "BanRecord",
      "description": "List of active kicks/bans",
      "type": "object",
      "properties": {
        "nickname": {
          "type": "string",
          "description": "The target name that is banned or kicked"
        },
        "kickEnd": {
          "type": "number",
          "description": "Timestamp when the ban expires in milliseconds"
        }
      },
      "required": ["nickname", "kickEnd"]
    }
  },
  "firestore": {
    "/messages/{messageId}": {
      "schema": "Message",
      "description": "Synchronized public chat message history"
    },
    "/users/{userId}": {
      "schema": "UserPresence",
      "description": "Active session presence listings and typing statuses"
    },
    "/kicks/{kickId}": {
      "schema": "BanRecord",
      "description": "Active ban records keyed by name"
    }
  }
}
